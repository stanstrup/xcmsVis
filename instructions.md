# Current Tasks

**See `CLAUDE.md` for development workflow, conventions, and resources.**


1) ✅ in the test setup_test_data() is run many times. Is it not possible for the tests to share this data so it is only run once?
2) ✅ Convert the vignette to quarto completely. make sure to update all references to the file.
