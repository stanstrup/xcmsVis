# Current Tasks

**See `CLAUDE.md` for development workflow, conventions, and resources.**

Move on to the remaining functions. Follow the same recipe. If you need to also get plots from Msnbase to make ggplot2 implementations work then do so. Use the dev-docs as your roadmap and keep them updated. Skip functions that are only for legacy objects.
