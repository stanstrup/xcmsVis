% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/gplot-LamaParama-methods.R,
%   R/gplot-methods.R
\name{gplot}
\alias{gplot}
\alias{gplot,LamaParama-method}
\alias{gplot,XChromatogram-method}
\alias{gplot,XChromatograms-method}
\alias{gplot,MChromatograms-method}
\title{\emph{ggplot2} Version of \code{plot()} for \code{XChromatogram}}
\usage{
gplot(x, ...)

\S4method{gplot}{LamaParama}(x, index = 1L, colPoints = "#00000060", colFit = "#00000080", ...)

\S4method{gplot}{XChromatogram}(
  x,
  col = "black",
  lty = 1,
  type = "l",
  peakType = c("polygon", "point", "rectangle", "none"),
  peakCol = "#00000060",
  peakBg = "#00000020",
  peakPch = 1,
  ...
)

\S4method{gplot}{XChromatograms}(
  x,
  col = "#00000060",
  lty = 1,
  type = "l",
  peakType = c("polygon", "point", "rectangle", "none"),
  peakCol = "#00000060",
  peakBg = "#00000020",
  peakPch = 1,
  ...
)

\S4method{gplot}{MChromatograms}(
  x,
  col = "#00000060",
  lty = 1,
  type = "l",
  peakType = c("polygon", "point", "rectangle", "none"),
  peakCol = "#00000060",
  peakBg = "#00000020",
  peakPch = 1,
  ...
)
}
\arguments{
\item{x}{A \code{LamaParama} object containing retention time alignment
parameters and results.}

\item{...}{Additional parameters (currently unused, for S4 compatibility).}

\item{index}{\code{integer(1)} specifying which retention time map to plot
(default: \code{1}).}

\item{colPoints}{Color for the matched peak points (default:
semi-transparent black).}

\item{colFit}{Color for the fitted model line (default:
semi-transparent black).}

\item{col}{Color for the chromatogram line (default: \code{"black"}). For
\code{XChromatograms} and \code{MChromatograms} objects, this can also be a
\strong{column name} from \code{pData(x)} to map color to sample metadata
(e.g., \code{col = "sample_group"}). When a column name is used,
ggplot2 automatically adds a color legend.}

\item{lty}{Line type for chromatogram (default: \code{1}).}

\item{type}{Plot type (default: \code{"l"} for line).}

\item{peakType}{\code{character(1)} defining the type of peak annotation:
\code{"polygon"}, \code{"point"}, \code{"rectangle"}, or \code{"none"}
(default: \code{"polygon"}).}

\item{peakCol}{Color for peak markers (default: \code{"#00000060"}). For
\code{XChromatograms} objects, this can also be a column name from
\code{pData(x)} to map peak border color to sample metadata.}

\item{peakBg}{Background color for peak markers (default: \code{"#00000020"}).
For \code{XChromatograms} objects, this can also be a column name from
\code{pData(x)} to map peak fill color to sample metadata.}

\item{peakPch}{Point character for peak markers when \code{peakType = "point"}
(default: \code{1}).}
}
\value{
A \code{ggplot} object.

A \code{ggplot} object.
}
\description{
Creates a \emph{ggplot2} version of a chromatogram with detected peaks marked.
This is equivalent to the base R \code{plot()} method for \code{XChromatogram} objects.

Creates a \emph{ggplot2} version of the retention time alignment model
visualization for \code{\link[xcms:LamaParama]{xcms::LamaParama()}} objects.
\code{LamaParama} objects contain parameters and results from landmark-based
retention time alignment.
}
\details{
This function creates a complete chromatogram plot with detected peaks
automatically marked, similar to the base R \code{plot()} method for
\code{XChromatogram} objects. If the chromatogram contains detected peaks,
they will be shown according to the \code{peakType} parameter.
\subsection{Color Mapping for Multi-Sample Chromatograms}{

When plotting \code{XChromatograms} or \code{MChromatograms} objects (multiple
samples), the \code{col}, \code{peakCol}, and \code{peakBg} parameters accept either:
\itemize{
\item A \strong{static color string} (e.g., \code{col = "red"}, \code{col = "#00000060"})
— all samples use the same color (default behavior).
\item A \strong{column name} from \code{pData(x)} as a quoted string (e.g.,
\code{col = "sample_group"}) — each sample is colored according to its
metadata value. The column must exist in \code{Biobase::pData(x)}, which
inherits from \code{sampleData()} of the parent \code{XcmsExperiment} object.
The value is automatically detected as a column name when it matches
a column in \code{pData(x)}.
}
}

This function visualizes the retention time alignment model for a specific
sample.

The plot shows:
\itemize{
\item Points representing matched chromatographic peaks between the sample
and reference
\item A fitted line (loess or GAM) showing the retention time correction model
}

The \code{LamaParama} object contains parameters for landmark-based alignment
including:
\itemize{
\item \code{method}: The fitting method ("loess" or "gam")
\item \code{span}: Span parameter for loess fitting
\item \code{outlierTolerance}: Tolerance for outlier detection
\item \code{zeroWeight}: Weight for the (0,0) anchor point
\item \code{bs}: Basis function for GAM fitting
\item \code{rtMap}: List of data frames with retention time pairs
}
}
\examples{
library(xcmsVis)
library(xcms)
library(faahKO)
library(MsExperiment)
library(ggplot2)

## Load preprocessed data
xdata <- loadXcmsData()
# Extract chromatogram
chr <- chromatogram(xdata, mz = c(200, 210), rt = c(2500, 3500))

# Plot with ggplot2
gplot(chr[1, 1])

# Plot data for all samples
gplot(chr)

# Color by sample metadata (requires pData column)
# gplot(chr, col = "sample_group")


library(xcmsVis)
library(xcms)
library(MsExperiment)

# LamaParama requires a reference dataset with landmarks
# See vignette("04-retention-time-alignment") for complete workflow

# Load reference and test datasets
ref <- loadXcmsData("xmse")
tst <- loadXcmsData("faahko_sub2")

# Extract landmarks from QC samples in reference
f <- sampleData(ref)$sample_type
f[f != "QC"] <- NA
ref_filtered <- filterFeatures(
    ref, PercentMissingFilter(threshold = 0, f = f))
ref_mz_rt <- featureDefinitions(ref_filtered)[, c("mzmed", "rtmed")]

# Create and apply LamaParama alignment
lama_param <- LamaParama(lamas = ref_mz_rt, method = "loess", span = 0.5)
tst_adjusted <- adjustRtime(tst, param = lama_param)

# Extract LamaParama result for visualization
proc_hist <- processHistory(tst_adjusted,
    type = xcms:::.PROCSTEP.RTIME.CORRECTION)
lama_result <- proc_hist[[length(proc_hist)]]@param

# Visualize the first sample's alignment
gplot(lama_result, index = 1)

}
\seealso{
\code{\link[xcms]{plot,XChromatogram,ANY-method}} for the original \emph{xcms}
implementation

\code{\link[xcms:LamaParama]{xcms::LamaParama()}}
}
\author{
Jan Stanstrup
}
