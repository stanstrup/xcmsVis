# Helper function to simulate feature grouping based on peak density

Helper function to simulate feature grouping based on peak density

## Usage

``` r
.simulate_feature_groups(pks, dens_x, dens_y, param)
```
