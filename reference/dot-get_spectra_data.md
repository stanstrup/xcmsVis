# Get spectra data from XCMS object

Internal helper to extract spectra/feature data from both XCMSnExp and
XcmsExperiment objects

## Usage

``` r
.get_spectra_data(object)
```

## Arguments

- object:

  XCMSnExp or XcmsExperiment object

## Value

data.frame with spectra data including retention times
